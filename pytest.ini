[pytest]
testpaths = tests
pythonpath = src
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
markers =
    live_api: tests that make live API calls (requires API key and costs money)
    asyncio: tests that use async/await